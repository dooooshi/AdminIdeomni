# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:2999/api

# CORS Configuration
# Comma-separated list of allowed origins for production
# Leave empty for development (will allow localhost and 127.0.0.1)
NEXT_PUBLIC_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Authentication Configuration
# These are used by the HTTP client for token storage
NEXT_PUBLIC_TOKEN_STORAGE_PREFIX=Ideomni_

# Optional: Custom CORS settings
# NEXT_PUBLIC_CORS_CREDENTIALS=false
# NEXT_PUBLIC_CORS_MAX_AGE=86400
